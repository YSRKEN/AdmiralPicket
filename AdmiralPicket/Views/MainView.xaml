<Window x:Class="AdmiralPicket.Views.MainView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Title="提督見張員" Height="200" Width="200">
	<Grid Margin="5,5,5,5">
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition/>
		</Grid.RowDefinitions>
		<Button Grid.Row="0" Content="座標取得" Command="{Binding GetPosition}"
				Width="64" Height="24" Margin="5,5,5,5"/>
		<TextBox Grid.Row="1" Text="{Binding LoggingText.Value, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"
				AcceptsReturn="True" IsReadOnly="True"/>
	</Grid>
</Window>
